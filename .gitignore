node_modules

.DS_Store

.vscode

test

# Dist folder should not be committed since it is generated, can be built from source
# and can change even if the source code does not change.
dist
dist-npm

# We can ignore the lock because the lib is built locally per monorepo.
pnpm-lock.yaml
